#include "main.h"
/**
 * add - function that adds two integers and returns the result
 * @x: receives first number
 * @y: receives second number
 * Return: addition of two integers
 */
int add(int x, int y)
{
	return (x + y);
}
