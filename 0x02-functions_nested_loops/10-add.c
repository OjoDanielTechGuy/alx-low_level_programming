#include "main.h"
/**
 * @x - receives first number
 * @y - receives second number
 * desc:function that adds two integers and returns the result
 * Return: addition of two integers
 */
int add(int x, int y)
{
	return (x + y);
}
