#include <stdio.h>
/**
 * main - program that prints its name
 * @argc: arguement count
 * @argv: arguement vector(strings)
 * Return: name print + \n
 */
int main(int argc, char *argv[])
{
	argc = 0

	printf("%s\n", argv[argc];

	return (0);
}
