#include <stdio.h>
/**
 * main - prints number of arguement
 * @argc: counts the arguement
 * @argv: array that leads to an arguement
 * Return: number of arguements
 */
int main(int argc, char *argv[])
{
	printf("%d\n", argc);

	return (0);
}
